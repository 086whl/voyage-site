<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.nuist.voyagesite.mapper.UserMapper">
    <select id="userNameIsExist" parameterType="java.lang.String" resultType="java.lang.Boolean" >
        select (case when count(*)=1 then 1 else 0 end) from tab_user where username=#{username}
    </select>
    <insert id="regist" parameterType="User">
        insert into tab_user(username,password,name,birthday,sex,telephone,email,status,code)
        values (#{username},#{password},#{name},#{birthday},#{sex},#{telephone},#{email},#{status},#{code})
    </insert>
</mapper>